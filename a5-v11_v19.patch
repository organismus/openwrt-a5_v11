diff --git a/target/linux/ramips/dts/A5-V11.dts b/target/linux/ramips/dts/A5-V11.dts
index 707963d..1044df5 100644
--- a/target/linux/ramips/dts/A5-V11.dts
+++ b/target/linux/ramips/dts/A5-V11.dts
@@ -93,7 +93,7 @@
 			partition@50000 {
 				compatible = "denx,uimage";
 				label = "firmware";
-				reg = <0x50000 0x3b0000>;
+				reg = <0x50000 0xfb0000>;
 			};
 		};
 	};
diff --git a/target/linux/ramips/image/rt305x.mk b/target/linux/ramips/image/rt305x.mk
index 59f0cbd..753d452 100644
--- a/target/linux/ramips/image/rt305x.mk
+++ b/target/linux/ramips/image/rt305x.mk
@@ -63,10 +63,10 @@ TARGET_DEVICES += 3g-6200nl
 
 define Device/a5-v11
   DTS := A5-V11
-  IMAGE_SIZE := $(ralink_default_fw_size_4M)
+  IMAGE_SIZE := $(ralink_default_fw_size_16M)
   IMAGES += factory.bin
   IMAGE/factory.bin := \
-	$$(sysupgrade_bin) | check-size $$$$(IMAGE_SIZE) | poray-header -B A5-V11 -F 4M
+	$$(sysupgrade_bin) | check-size $$$$(IMAGE_SIZE) | poray-header -B A5-V11 -F 16M
   DEVICE_TITLE := A5-V11
   DEVICE_PACKAGES := kmod-usb-core kmod-usb-ohci kmod-usb2
   DEFAULT := n
diff --git a/tools/firmware-utils/src/mkporayfw.c b/tools/firmware-utils/src/mkporayfw.c
index 6ec4f32..b986d22 100644
--- a/tools/firmware-utils/src/mkporayfw.c
+++ b/tools/firmware-utils/src/mkporayfw.c
@@ -136,7 +136,10 @@ static struct flash_layout layouts[] = {
 		.id		= "8M",
 		.fw_max_len	= 0x7c0000,
 	}, {
-		/* terminating entry */
+                .id             = "16M",
+                .fw_max_len     = 0xfc0000,
+         }, {
+ 		 /* terminating entry */
 	}
 };
 
@@ -146,6 +149,16 @@ static struct board_info boards[] = {
 		.hw_id          = HWID_A5_V11,
 		.layout_id      = "4M",
 		.key            = KEY_A5_V11,
+        }, {
+                .id             = "A5-V11",
+                .hw_id          = HWID_A5_V11,
+                .layout_id      = "8M",
+                .key            = KEY_A5_V11,
+        }, {
+                .id             = "A5-V11",
+                .hw_id          = HWID_A5_V11,
+                .layout_id      = "16M",
+                .key            = KEY_A5_V11,
         }, {
 		.id		= "MPR-A1",
 		.hw_id		= HWID_HAME_MPR_A1_L8,
diff -Nu a/files.orig/etc/uci-defaults/99-custom b/files/etc/uci-defaults/99-custom
--- a/files.orig/etc/uci-defaults/99-custom	1970-01-01 00:00:00.000000000 +0000
+++ b/files/etc/uci-defaults/99-custom	2023-01-13 15:44:59.053702569 +0000
@@ -0,0 +1,40 @@
+[ "$(uci -q get system.@system[0].zonename)" = "Europe/Kyiv" ] && exit 0
+
+uci -q batch << EOF
+set system.@system[0].zonename='Europe/Kyiv'
+add system led
+set system.@led[-1].sysfs='red:power'
+set system.@led[-1].trigger='timer'
+set system.@led[-1].delayon='100'
+set system.@led[-1].delayoff='5000'
+set luci.main.mediaurlbase='/luci-static/openwrt.org'
+set system.@system[-1].hostname='router'
+set system.@system[-1].zram_size_mb='4'
+
+add system led
+set system.@led[-1].sysfs='blue:system'
+set system.@led[-1].trigger='netdev'
+set system.@led[-1].dev='wlan0'
+set system.@led[-1].mode='rx'
+
+set network.wan=interface
+set network.wan.proto='none'
+set network.wan.proto='dhcp'
+set network.wan.device='eth0'
+
+set network.@device[0].ports='wlan0'
+
+set wireless.radio0.disabled='0'
+del wireless.radio0.htmode
+set wireless.radio0.channel='auto'
+set wireless.radio0.cell_density='0'
+
+set wireless.default_radio0.mode='ap'
+set wireless.default_radio0.ssid='OpenWrt'
+set wireless.default_radio0.encryption='none'
+set wireless.default_radio0.network='lan'
+
+add_list dhcp.wan.ra_flags='none'
+
+commit
+EOF
+
